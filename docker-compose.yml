services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: ${SECRET_KEY}
    volumes:
      - instance:/app/instance
    restart: unless-stopped

volumes:
  instance:
