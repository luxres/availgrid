services:
  web:
    build: .
    pull_policy: build
    ports:
      - "${PORT}:5000"
    environment:
      SECRET_KEY: ${SECRET_KEY}
    volumes:
      - instance:/app/instance
    restart: unless-stopped

volumes:
  instance:
